{
  "name": "cars-api",
  "version": "1.0.0",
  "description": "Backend API for cars app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}